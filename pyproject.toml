[project]
name = "obsidian-cli"
version = "0.1.0"
description = "Various obsidian cli tools - Mainly high performance searching / indexing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.5",
    "pyyaml>=6.0.3",
    "rapidyaml>=0.10.0",
    "ryaml>=0.4.0",
]
